const apiPerformance = {
  coinbase: { success: 0, failures: 0, avgResponseTime: 0 },
  solana: { success: 0, failures: 0, avgResponseTime: 0 },
  // ... other services
};

function trackAPIPerformance(service, success, responseTime) {
  // Implementation to track and display API performance
}
